# Backend API

A minimal Node.js/Express server is set up here to support future features.

## Setup
1. `cd backend`
2. `npm install express`
3. `node server.js`

## Purpose
This backend currently serves as a foundation for:
- User Authentication
- Cloud Application Storage
- Collaborative Editing (WebSockets)
